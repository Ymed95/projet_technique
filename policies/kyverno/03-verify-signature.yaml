# Politique 3 — Exiger une signature cosign VALIDE de VOTRE identité.
# C'est le contrôle central : le cluster n'exécute que des images signées par vous.
#
# Deux variantes ci-dessous :
#   (A) mode PAR CLÉ    → collez votre cosign.pub (Lab 2)
#   (B) mode KEYLESS    → exigez l'identité du workflow GitHub (Lab 5)  [décommentez]
#
# ➜ Adaptez "ghcr.io/ymed95/scs-demo-app" et la clé / l'identité.
apiVersion: kyverno.io/v1
kind: ClusterPolicy
metadata:
  name: verify-image-signature
  annotations:
    policies.kyverno.io/title: Vérifier la signature cosign
    policies.kyverno.io/category: Supply Chain Security
    policies.kyverno.io/description: >-
      Refuse toute image non signée ou signée par une identité non autorisée.
spec:
  validationFailureAction: Enforce
  background: false
  webhookTimeoutSeconds: 30
  rules:
    - name: verifier-signature-cosign
      match:
        any:
          - resources:
              kinds: [Pod]
      verifyImages:
        - imageReferences:
            - "ghcr.io/ymed95/scs-demo-app*"
          mutateDigest: true          # réécrit le tag en digest (empêche la substitution)
          verifyDigest: true
          required: true
          # ---- MODE KEYLESS (SLSA L2) : on exige l'identité du workflow GitHub Actions ----
          # L'image légitime est celle SIGNÉE PAR NOTRE PIPELINE (OIDC du runner), pas par une
          # clé locale. Un attaquant ne peut pas se faire passer pour ce workflow précis.
          attestors:
            - count: 1
              entries:
                - keyless:
                    issuer: "https://token.actions.githubusercontent.com"
                    subject: "https://github.com/Ymed95/projet_technique/.github/workflows/supply-chain.yml@refs/heads/main"
                    rekor:
                      url: "https://rekor.sigstore.dev"
